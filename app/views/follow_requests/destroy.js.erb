<%# grab the unfollow %>
var unfollow = $("#<%= dom_id(@follow_request) %>")

<%# get the form %>
var form = $("<%= j(render "follow_requests/form", follow_request: @follow_request) %>");

$(".<%= dom_id(@follow_request)%>").replaceWith(form);
